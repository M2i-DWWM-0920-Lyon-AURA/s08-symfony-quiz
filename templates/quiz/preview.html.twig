<div class="card">
    <h5 class="card-header">{{ quiz.title }}</h5>
    <div class="card-body">
        <p class="card-text">{{ quiz.description }}</p>
        <a href="/quiz/{{ quiz.id }}" class="btn btn-outline-primary">Voir plus</a>
    </div>
</div>
